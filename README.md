use localhost:3000 to acess the app

use this command to setup the app

docker compose up --build

this will make the image for the first time.
to use repeat times use

docker compose up

to run the app

the github link is this
https://github.com/cterry311/dockerized_python_API_with_frontend.git