services:
#  frontend:
#    build: ./front_end
#    ports:
#      - "3000:3000"
#    depends_on:
#      - app
  app:
    build: app

    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres:15
    ports:
      - "5432:5432"